# Mali core

This package contains Mali's deterministic tutoring rules. It has no runtime
dependencies and does not perform network, file, database, or model calls.

```bash
uv sync --all-groups
uv run pytest
```
